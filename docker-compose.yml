version: '3.8'

services:
  postgres:
    image: ankane/pgvector:latest
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: code_factory
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  kanban-ui:
    # Usando Planka ou Plane como representação da interface Kanban
    image: ghcr.io/plankanban/planka:latest
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://admin:admin@postgres:5432/code_factory
    depends_on:
      - postgres

  orchestrator:
    build: ./orchestrator
    environment:
      - REDIS_URL=redis://redis:6379/0
      - LLM_API_KEY=${LLM_API_KEY}
    ports:
      - "8080:8080"
    depends_on:
      - redis

  worker-queue:
    build: ./orchestrator
    command: celery -A core.tasks worker --loglevel=info
    environment:
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      - redis

  agents:
    build: ./agents
    environment:
      - OPENCLAW_URL=http://openclaw:8081
      - MCP_URL=http://mcp-server:8000
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      - openclaw
      - mcp-server

  mcp-server:
    build: ./mcp-server
    environment:
      - DB_URL=postgresql://admin:admin@postgres:5432/code_factory
    ports:
      - "8000:8000"
    depends_on:
      - postgres

  openclaw:
    # Framework executor de ações
    image: openclaw/executor:latest
    privileged: true # Necessário se for executar docker-in-docker
    environment:
      - WORKSPACE_DIR=/workspace
    volumes:
      - ./workspace:/workspace
      - /var/run/docker.sock:/var/run/docker.sock

volumes:
  pg_data: